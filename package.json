{
  "name": "node-shopify-product-variant",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && esbuild index.ts --bundle --minify --sourcemap --platform=node --target=node18 --outdir=dist",
    "package": "rm -rf dist/index.zip && zip -rj dist/index.zip ./dist/*",
    "deploy": "aws lambda update-function-code --function-name shopify-product-variant --zip-file fileb://dist/index.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@shopify/shopify-api": "^7.7.0",
    "@types/aws-lambda": "^8.10.122"
  },
  "devDependencies": {
    "esbuild": "^0.19.3"
  }
}
